<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smallbell.modules.app.dao.AddressDao">

    <sql id="Base_Column_List">
        id, user_id as userId, consignee, phoneNumber, address, detailed_address as detailedAddress, isOn,area_code as areaCode
    </sql>

    <sql id="Example_Where_Clause">
        <where>
            <foreach collection="oredCriteria" item="criteria" separator="or">
                <if test="criteria.valid">
                    <trim prefix="(" prefixOverrides="and" suffix=")">
                        <foreach collection="criteria.criteria" item="criterion">
                            <choose>
                                <when test="criterion.noValue">
                                    and ${criterion.condition}
                                </when>
                                <when test="criterion.singleValue">
                                    and ${criterion.condition} #{criterion.value}
                                </when>
                                <when test="criterion.betweenValue">
                                    and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                                </when>
                                <when test="criterion.listValue">
                                    and ${criterion.condition}
                                    <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                                        #{listItem}
                                    </foreach>
                                </when>
                            </choose>
                        </foreach>
                    </trim>
                </if>
            </foreach>
        </where>
    </sql>

    <select id="selectByPrimaryKey" resultType="com.smallbell.modules.app.entity.UserAddressListEntity">
        select
        <include refid="Base_Column_List" />
        from user_address_list
        where id = #{id}
    </select>

    <insert id="insert" parameterType="com.smallbell.modules.app.entity.UserAddressListEntity" useGeneratedKeys="true" keyProperty="id">
        insert into user_address_list (id, user_id, consignee,
                                       phoneNumber, address, detailed_address,
                                       isOn,area_code)
        values (#{id}, #{userId}, #{consignee},
                #{phoneNumber}, #{address}, #{detailedAddress},
                #{isOn},#{areaCode})
    </insert>

    <update id="updateByPrimaryKey" parameterType="com.smallbell.modules.app.entity.UserAddressListEntity" useGeneratedKeys="true" keyProperty="id">
        update user_address_list
        set user_id = #{userId},
            consignee = #{consignee},
            phoneNumber = #{phoneNumber},
            address = #{address},
            detailed_address = #{detailedAddress},
            isOn = #{isOn},
            area_code = #{areaCode}
        where id = #{id}
    </update>

    <select id="selectByExample" parameterType="com.smallbell.modules.app.form.address.UserAddressListExample" resultType="com.smallbell.modules.app.entity.UserAddressListEntity">
        select
        <if test="distinct">
            distinct
        </if>
        <include refid="Base_Column_List" />
        from user_address_list
        <if test="_parameter != null">
            <include refid="Example_Where_Clause" />
        </if>
        <if test="orderByClause != null">
            order by ${orderByClause}
        </if>
    </select>

</mapper>